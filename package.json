{
  "name": "sabor-static-image-builder",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18.17.0"
  },
  "scripts": {
    "images:scan": "node ./scripts/fetch-images.mjs --mode=scan",
    "images:build": "node ./scripts/fetch-images.mjs --mode=build"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "sharp": "^0.33.5"
  }
}
